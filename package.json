{
  "name": "amber-streetwear",
  "version": "1.0.0",
  "private": true,
  "description": "AMBER Streetwear PWA + Capacitor app",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "node --env-file=.env.local server/server.js"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  },
  "dependencies": {
    "stripe": "^22.5.0"
  }
}
